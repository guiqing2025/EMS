---
description: EMS 双机约定 — Windows 生产 + Mac 开发
alwaysApply: true
---

# EMS 环境约定（双机）

| | 生产（权威） | 开发 |
|--|--|--|
| 机器 | **Windows** | **Mac** |
| 地址 | Windows 本机 IP `:8000`（切流后替换原 `192.168.2.168:8000`） | `5173` / `8001` |
| 数据库 | Postgres `ems_prod`（`backend/.env.postgres`） | `ems_dev` 或 `ems.dev.db` |
| 启动 | `backend\start_ems.ps1` | `start_dev_env.sh` / `start_ems_dev.sh` |
| 同步 | `scripts\sync.ps1` | `scripts/sync.sh` |
| 本机路径 | `env/local.env`（由 `env/env.windows.example` 生成） | `env/local.env`（由 `env/env.mac.example` 生成） |

切流步骤见 [docs/dual_machine_cutover.md](../../docs/dual_machine_cutover.md)。

## 对 Agent 的约束

1. **默认改代码在 Mac 开发验证**（8001 / 开发库）；用户明确要求改生产或当前仍在 Mac 临时代跑生产时，按其指示在生产验证。
2. **切流完成前**：若生产仍在本机 Mac `8000`，可按用户授权继续在 Mac 生产验证；切流后生产只在 Windows。
3. **禁止**：把开发库拷进 `ems_prod`；用 `EMS_USE_SQLITE=1` + `ems.dev.db` 冒充生产 8000。
4. 启动生产前清掉 `EMS_DEV_MODE` / `EMS_DB=ems.dev.db`。
5. 库方向：**仅 Windows → Mac 单向**（`export_db.ps1` / `import_db_dev.sh`），禁止默认反向灌生产。
6. 机器路径走 `env/local.env` / 环境变量，不要再写死 `/Users/dx/Desktop/...` 进业务代码。

## 心智模型

- 同一套 Git 源码；业务权威数据在 **Windows `ems_prod`**。
- Mac 负责开发与联调；需要真实数据时用 dump 刷开发库。
