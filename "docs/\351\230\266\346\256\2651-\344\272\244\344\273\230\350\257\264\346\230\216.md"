# 阶段 1 交付说明 — 售前闭环

## 里程碑 M1

**询价 → 成本报价 → 确认 → 转打样订单 / 销售订单草稿** 已打通。

## 已上线

| 菜单 | 路径 | 能力 |
|------|------|------|
| 询价单 | `/presales/inquiry` | 建单、明细行、转报价 |
| 成本报价/报价单 | `/quotation` | 原核算+供应商成本/卖价；确认后转设计/打样单/销售单 |
| 设计/打样 | `/presales/design` | 任务登记与确认 |
| 样品邮寄/链接 | `/presales/sample-mail` | 邮寄/链接登记 |
| 借样单 | `/presales/sample-loan` | 借出/还入状态 |
| 打样订单 | `/sales/sample-orders` | 报价转入后的单头列表 |
| 销售订单 | `/sales/orders` | 报价转入的草稿单头（阶段2再扩） |

## 自测约定

每一阶段交付后，用模拟数据跑脚本验收，不依赖真实客户数据。

- 阶段 0+1：`backend/scripts/sim_phase01_presales.py`
- 要求：通过项全绿、负向用例（未确认报价不可转销售）通过、HTTP 冒烟通过

最近一次（2026-09-09 复跑）：`通过 20 / 失败 0`（服务层 + `/api`）。

## 建议验证路径（界面）

1. 主数据建一个客户（可选）
2. **售前 → 询价单** 新建 → 加一行料 → 保存 → **转报价**
3. 报价页填对外卖价 → 导入 BOM 核算（或手工把状态做到已核算）→ **确认报价**
4. 点 **转销售订单** / **转打样订单** / **转设计打样**
5. 到对应菜单看草稿单号

## API 前缀

`/api/presales/*`
