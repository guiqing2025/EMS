# 阶段 0 交付说明 — 流程图节点对照（已确认开工）

> 依据：`docs/景立ERP改造执行计划-以流程图为准.md`  
> 状态日期：阶段 0 落地

## 0.1 节点对照结论

| 流程图节点 | 系统动作 | 阶段 |
|------------|----------|------|
| 客户资料 / 供应商 / 价格 / 库存产品 | **新建** 主数据 CRUD | 0 ✅ |
| 良品/待检/退货仓 | **新建** 仓库维度 | 0 ✅ |
| 单据编号规则 | **新建** 序列表 | 0 ✅ |
| 泳道角色 业务/采购/生产/品质/PMC/财务/仓管 | **扩展** 种子账号 | 0 ✅ |
| 菜单按流程图重排 | **改造** 骨架；待建标「待建」 | 0 ✅ |
| 历史客户 PO（SrmOrder） | **降级** 菜单「历史·客户订单(过渡)」；新单不强制 SRM | 0 ✅ |
| 询价→销售订单…财务 | **待建** 占位页挂菜单 | 1–10 |

## 0.2 已上线页面

- `/master/customers` 客户资料
- `/master/suppliers` 供应商
- `/master/prices` 价格管理
- `/master/stock-products` 库存产品
- `/master/warehouses` 仓库维度
- `/master/doc-numbers` 单据编号（可试号）

## 0.3 API

- `GET/POST /api/master/*`
- `GET /api/master/bootstrap` 种子仓库 + 单号规则

## 0.4 种子账号（角色）

除原有账号外新增（默认密码 `888888`，仅开发环境）：

| 用户名 | 角色 |
|--------|------|
| sales | 业务 |
| purchasing | 采购 |
| production | 生产 |
| quality | 品质 |
| finance | 财务 |

## 0.5 下一步

进入 **阶段 1：售前闭环**（询价单 → 成本报价 → 报价单 → 打样/借样）。
